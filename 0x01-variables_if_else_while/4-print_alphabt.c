#include <stdio.h>

/**
 * main - Entry point
 *
 * Discription: print alphabets
 *
 * Return: Always (successful)
 */

int main(void)
{
	char d;

	d = 'a';

	while (d <= 'z')
	{
	if (d == 'e' || d == 'q');
		++d;

		{
			putchar (d);
		}
	}
	putchar ('\n');
	
	return (0);
}







